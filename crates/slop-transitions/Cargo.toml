[package]
name = "slop-transitions"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Video transition library. Emits FFmpeg xfade/acrossfade filtergraph fragments for the V2 schema's TransitionItem."

[dependencies]
slop-core = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
