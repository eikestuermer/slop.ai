# Application icons

Placeholder. Replace before public release.

- `32x32.png`, `128x128.png`, `128x128@2x.png` for Linux / generic uses
- `icon.icns` for macOS
- `icon.ico` for Windows

Until real icons are produced, the Tauri build will warn and substitute its
default. That is fine for development but should block the V1 release per
the [release checklist](../../../docs/release-checklist.md).
